#include <iostream>
#include <fstream>
#include <vector>
#include <string>
#include <sstream>
#include <map>
#include <set>
#include "zipfs.h"


void computeWordFrequency(const std::vector<char>& book) {
    int countUniqueWords(const std::vector<char>& book);
}